package org.selenium.practice.driver;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class Driver {
    public void createWebdriver() {
        WebDriver setup = new ChromeDriver();
    }
}
